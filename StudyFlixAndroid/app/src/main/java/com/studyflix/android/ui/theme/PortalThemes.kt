package com.studyflix.ui.theme

import com.studyflix.android.ui.theme.AdminColors
import com.studyflix.android.ui.theme.PortalTheme
import com.studyflix.android.ui.theme.StudentColors
import com.studyflix.android.ui.theme.TeacherColors

val StudentTheme = PortalTheme(
    primary = StudentColors.Primary,
    secondary = StudentColors.Secondary,
    accent = StudentColors.Accent
)

val TeacherTheme = PortalTheme(
    primary = TeacherColors.Primary,
    secondary = TeacherColors.Secondary,
    accent = TeacherColors.Accent
)

val AdminTheme = PortalTheme(
    primary = AdminColors.Primary,
    secondary = AdminColors.Secondary,
    accent = AdminColors.Accent
)